## Pointer and Reference in C++

### Why??
* Memory has address, using pointer (*) and reference (&) allow us to access directly the memory address
* Goal of using pointer is for better memory management

### How??
* Pointer always an integer data type (4 byte)