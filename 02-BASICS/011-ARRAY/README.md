## Array in C++

### How to define array?
    ```cpp
    data_type[n]
    ```
    index of array in C++ start from 0
    array will produce memory sequantially, in descending or ascending order => this is the different when we just define some variables (random address)
    array in C++ is a basic array so you can't get the number of members (exccept using the standard library array)