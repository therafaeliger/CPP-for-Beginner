#include <iostream> // library iostream => so you can use std
// empty line not executed

// this is a comment

/*
    this is
    multi line
    comment
*/

int main( /* input argument */ ) // main function
{ // body statement
    std::cout << "Hi, this is rafaeliger!" << std::endl; // => statement, must ended with semicolon (;)
    std::cin.get(); // wait until a key pressed
    return 0;
}